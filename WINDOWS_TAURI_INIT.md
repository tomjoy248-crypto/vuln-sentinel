# Windows 桌面壳初始化方案（Tauri 优先）

## 现状

仓库当前只有 Web 前端，没有 `src-tauri`、`electron` 或现成桌面壳配置。

## 推荐选择

### 方案 A：Tauri（推荐）

适合当前项目的原因：

- 直接复用现有 Vite 前端
- 安装包更轻
- 更适合后续做企业版桌面分发
- 与 Web 版共用同一套账号、套餐和结果页

### 方案 B：Electron

适合快速出第一版，但体积更大。

## 初始化步骤

1. 安装 Rust 和 Tauri CLI
2. 在仓库根目录创建 `src-tauri/`
3. 将 `frontend/dist` 作为桌面壳加载目标
4. 复用现有登录、扫描、结果、套餐、审计页面
5. 配置 Windows 图标、应用名、版本号
6. 打包 `.msi` 或 `.exe`

## 第一版功能边界

- 登录 / 注册
- 输入网址扫描
- 查看结果与证据
- 套餐购买与积分查看
- 审计与工单
- 导出报告

## 建议的技术目标

- 保持 Web 与桌面壳共用后端 API
- 桌面壳只做容器，不重写业务逻辑
- Windows 版先做稳定交付，再考虑自动更新、托盘和离线缓存

## 下一步

如果要开始正式做 Windows 版，建议先完成：

- Web 商业版稳定
- PWA 安装体验稳定
- Tauri 桌面壳最小骨架
