[package]
name = "vuln-sentinel-desktop"
version = "11.0.0"
edition = "2021"

[build-dependencies]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[package.metadata.tauri]
# Placeholder manifest for the Windows desktop shell.
