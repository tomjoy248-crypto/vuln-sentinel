# 漏洞哨兵 11-S - Python 依赖
# 安装：pip install -r requirements.txt

# Web 框架
fastapi>=0.100
uvicorn[standard]>=0.23

# 生产环境 WSGI/ASGI 服务器
gunicorn>=21.0

# HTTP 客户端（异步扫描用）
httpx>=0.25
h2>=4.0  # 启用 httpx HTTP/2 支持

# 数据验证与配置
pydantic>=2.0
pydantic-settings>=2.0

# 数据库（PostgreSQL 生产环境，SQLAlchemy 连接池）
sqlalchemy>=1.4
psycopg2-binary>=2.9

# JWT 认证
PyJWT>=2.8

# 密码哈希
bcrypt>=4.0

# DNS 验证（域名归属验证）
dnspython>=2.6

# PDF 报告生成
reportlab>=4.0

# 定时任务（监控扫描）
apscheduler>=3.10

# 加密（SSO 凭证存储 / Fernet）
cryptography>=41.0

# 结构化日志
structlog>=24.0

# Prometheus 指标
prometheus-fastapi-instrumentator>=7.0
prometheus-client>=0.20

# Redis（分布式限流 / 异步任务队列）
redis>=5.0

# Sentry 错误追踪（可选）
sentry-sdk[fastapi]>=2.0

# 单元测试（开发用）
pytest>=7.0
pytest-asyncio>=0.21
pytest-cov>=4.0
mypy>=1.8
bandit[toml]>=1.7
pre-commit>=3.6

