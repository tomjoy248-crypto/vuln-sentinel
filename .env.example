# 漏洞哨兵 11-S 生产环境配置示例
# 复制本文件为 .env 后按需填写，切勿将真实 .env 提交到版本库。

# ---------------------------------------------------------------------------
# 基础环境
# ---------------------------------------------------------------------------
ENV=production
PRODUCTION=1
HOST=0.0.0.0
PORT=8000

# ---------------------------------------------------------------------------
# 安全：JWT（生产环境必须设置，长度不少于 32 字符）
# ---------------------------------------------------------------------------
JWT_SECRET=change-me-to-a-random-string-at-least-32-chars
JWT_EXPIRE_SECONDS=86400

# ---------------------------------------------------------------------------
# 安全：CORS（生产环境必须显式列出允许来源，禁止通配符 *）
# ---------------------------------------------------------------------------
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# ---------------------------------------------------------------------------
# 数据库（默认使用 SQLite；生产建议切换到 PostgreSQL）
# ---------------------------------------------------------------------------
# DATABASE_URL=postgresql://user:password@localhost:5432/vuln_sentinel
DB_DIR=/data
DB_NAME=scans.db

# ---------------------------------------------------------------------------
# Redis（用于分布式限流、异步任务队列；生产强烈建议启用）
# ---------------------------------------------------------------------------
# REDIS_URL=redis://localhost:6379/0

# ---------------------------------------------------------------------------
# 日志与可观测性
# ---------------------------------------------------------------------------
LOG_LEVEL=INFO
ENABLE_STRUCTLOG=1
# SENTRY_DSN=https://xxx@yyy.ingest.sentry.io/zzz

# ---------------------------------------------------------------------------
# 扫描策略
# ---------------------------------------------------------------------------
SCAN_TIMEOUT=12
MAX_CRAWL_PAGES=8
# 默认关闭公网试用；确认资源、授权范围和限流策略后再显式开启
PUBLIC_DEMO_ENABLED=0
FREE_TRIAL_ENABLED=0

# ---------------------------------------------------------------------------
# 限流（每分钟请求数）
# ---------------------------------------------------------------------------
RATE_LIMIT_GLOBAL_PER_MINUTE=30
RATE_LIMIT_SCAN_PER_MINUTE=10
RATE_LIMIT_FIX_PER_MINUTE=10

# 自动巡检
# ---------------------------------------------------------------------------
PATROL_INTERVAL_HOURS=6
PATROL_SCORE_REGRESSION_THRESHOLD=10

# ---------------------------------------------------------------------------
# 计费与支付网关（可选）
# ---------------------------------------------------------------------------
# 服务对外地址，用于支付成功回调与跳转
PUBLIC_BASE_URL=https://your-domain.com

# Stripe（可选）：https://dashboard.stripe.com/apikeys
# STRIPE_PUBLISHABLE_KEY=pk_live_xxx
# STRIPE_SECRET_KEY=sk_live_xxx
# Stripe Webhook Endpoint 签名密钥
# STRIPE_WEBHOOK_SECRET=whsec_xxx

# 支付宝 / 微信支付：接入前需准备商户号、私钥、公钥等，当前已预留回调接口
# ALIPAY_APP_ID=
# ALIPAY_PUBLIC_KEY=
# WECHAT_MCH_ID=
# WECHAT_API_V3_KEY=
# 开发测试：开启 mock 模式可直接回调到账（仅用于测试环境）
# ALIPAY_MOCK=true
# WECHAT_MOCK=true

# ---------------------------------------------------------------------------
# LLM（可选，用于智能修复建议）
# ---------------------------------------------------------------------------
LLM_ENABLED=0
LLM_PROVIDER=openai
LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# ---------------------------------------------------------------------------
# 高级：TLS 证书校验（生产环境保持 1）
# ---------------------------------------------------------------------------
TLS_VERIFY=1

# ---------------------------------------------------------------------------
# 监控与指标
# ---------------------------------------------------------------------------
# METRICS_AUTH_TOKEN=generate-a-long-random-token
# METRICS_PUBLIC=0

# ---------------------------------------------------------------------------
# 数据保留策略（自动清理过期数据）
# ---------------------------------------------------------------------------
DATA_RETENTION_SCANS_DAYS=90
DATA_RETENTION_AUDIT_LOGS_DAYS=180
DATA_RETENTION_USAGE_LOGS_DAYS=60

# ---------------------------------------------------------------------------
# 数据备份
# ---------------------------------------------------------------------------
BACKUP_DIR=/data/backups
BACKUP_RETENTION_COUNT=30

# ---------------------------------------------------------------------------
# 邮箱验证与密码重置（需要 SMTP 配置）
# ---------------------------------------------------------------------------
EMAIL_VERIFICATION_REQUIRED=0
PASSWORD_RESET_ENABLED=1

# ---------------------------------------------------------------------------
# 生产环境禁用 API 文档
# ---------------------------------------------------------------------------
DISABLE_API_DOCS=1
