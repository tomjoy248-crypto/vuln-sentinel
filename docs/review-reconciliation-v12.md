# v12.0.0 评审清单核对

这份核对用于区分历史记录与当前代码，避免把已修复事项重复列为“未动”。

| 评审项 | 当前状态 | 证据 |
| --- | --- | --- |
| `main.py` 体量 | 未完成，仍需分阶段拆分 | 当前约 931 KB；整体迁移尚未开始 |
| 只扫首页 | 已部分修复 | `MAX_CRAWL_PAGES` 默认 20；同源链接、脚本和公开 API 入口发现已接入 |
| AI 顾问命名 | 已修复定位 | README/STATUS 明确为“规则引擎 + 可选 LLM”，前端提示已更新 |
| SSH 自动修复风险 | 已修复默认行为 | 默认 dry-run；只有显式 `apply=true` 才远程写入 |
| 版本号冲突 | 发布版本已统一 | 桌面发布版本为 `v12.0.0`；`11-S` 仅保留为后端兼容代号和历史 CHANGELOG 标题 |
| Tauri 桌面端 | 可构建的 NSIS 打包壳 | CI 已执行 Tauri build、安装生命周期和卸载检查；仍不是完整原生重构 |
| 根目录文档过载 | 已部分整理 | 中间性质文档已移入 `docs/archive/`，核心文档仍保留在根目录 |

## 后续唯一 P0

`main.py` 拆分应采用“先抽离、再替换、每批回归”的方式推进：先建立 `app/routers`、`app/detectors`、`app/reports` 的稳定接口，再逐模块迁移，避免一次性重构导致发布回归。
