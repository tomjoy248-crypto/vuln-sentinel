# Windows 客户端启动体验

## 默认启动效果
- 窗口标题显示为 `Vuln Sentinel`。
- 启动后直接进入 Web 版首页。
- 保持与 Web 版一致的登录、扫描、套餐、审计入口。
- 启动态、黑屏态和错误态要给出明确提示，避免用户以为程序无响应。

## 当前目标
让 Windows 壳看起来像一个正式客户端，而不是单纯桌面壳。

## 需要保留的体验
- 启动页面不能长期卡在空白或黑屏。
- 登录失败、后端未启动、端口冲突时要明确提示。
- 扫描入口与结果页要能正常接管主界面。
- 对可疑结果要展示“建议复核”而不是直接下结论。

## 体验原则
- 能用优先。
- 结果可信优先。
- 误报提示优先。
- 品牌、标题、入口命名保持一致。
