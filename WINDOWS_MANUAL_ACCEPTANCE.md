# Windows 人工验收清单

自动化构建和安装生命周期检查通过后，应在一台未安装本软件的 Windows 计算机上完成以下验收。每项记录版本、时间和结果，失败项不得标记为通过。

## 安装和启动

- 从 GitHub Release 下载当前版本安装包并记录文件大小和校验值。
- 安装到默认目录，确认安装程序正常结束且开始菜单快捷方式可用。
- 启动软件，确认首页版本号与安装包版本一致。
- 执行一次本地演示扫描，确认结果页、证据、报告导出和复测入口可用。

## 升级和卸载

- 保留旧版本数据，安装当前版本，确认升级不会覆盖历史结果和配置。
- 重启软件后确认历史记录仍可读取。
- 使用系统卸载入口卸载，确认产品文件已移除。
- 如需保留审计记录，先按产品策略导出，再清理用户数据。

## 认证和业务流程

- 只使用明确授权的测试目标和测试账号。
- 使用两个不同权限账号导入请求上下文，检查权限差异结果和脱敏证据。
- 导入一条完整业务流程和测试数据，检查状态跳跃、重复提交和复测结果。
- 未提供账号或业务样本时，应记录为未验收，不得宣称自动完成。

## 队列和环境

- 未配置 Redis 时验证本地队列的暂停、恢复、取消和失败重试。
- 配置测试 Redis 后验证服务重启、处理中任务恢复和任务状态一致性。
- 记录 Windows 版本、Python 运行时、Redis 版本和网络限制，便于复现问题。
