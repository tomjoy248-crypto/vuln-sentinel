# 安全策略

## 支持版本

| 版本 | 支持状态 |
| --- | --- |
| 当前版本 | ✅ 积极维护 |
| 更早版本 | ❌ 不再维护 |

## 报告漏洞

如果你发现 Vuln Sentinel 本身的安全漏洞，请不要通过公开 issue 报告，而是通过私下渠道联系维护者。

请尽量提供：
1. 漏洞简要描述
2. 复现步骤
3. 潜在影响
4. 联系方式

我们会在收到报告后 48 小时内回复，并在确认后尽快修复。

## 已知安全限制

- 仅检测已授权网站。
- `PUBLIC_DEMO_ENABLED` 默认关闭，生产环境如需公开演示入口请明确开启并单独评估授权边界。
- `JWT_SECRET` 在生产环境部署前必须修改。
- `LLM_API_KEY` 会写入配置，但启用 `repr=False` 防止日志泄露；仍建议使用 secret 管理器而不是 `.env`。
- 自动巡检功能会以系统身份访问外部站点，生产环境请评估合规风险。

## 依赖安全

我们使用 GitHub Dependabot（配置在 `.github/dependabot.yml`）自动检测依赖漏洞。
